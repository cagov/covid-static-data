name: Upload changed files To Azure Blob Storage
on:
  push:
    branches:
      - main

jobs:
  changes:
    runs-on: ubuntu-latest
    steps:
      # Checkout the repo
      - uses: actions/checkout@v2

      - name: list stuff
        run: ls -R 
